{# this is a comment; we want to see if twig parses it in any way #}

{# this comment includes annotations
    @Field(name="greeting", contents="hello")
    @Field(name="name", contents="jhb")
#}

{{ greeting }} {{ name }}! This is an {{ environment.emotion }} test of the template scanning system.

{% for item in list %}
    {{ item.subitem.subsubitem }}

    {% for nestitem in item.sublist %}
        {{ nestitem.attribute }}
    {% endfor %}
{% endfor %}

{% for alsoitem in list %}
    {{ alsoitem.attribute }}
{% endfor %}

{% block blockname %}

{% endblock %}

{% include 'MalwarebytesTemplateBundle:Test:included-example.html.twig' %}

{% include 'MalwarebytesTemplateBundle:Test:included-context-example.html.twig' with {'contextitem': onlyincontext } %}